# Publish TDD

Given the approved TDD text and the firm config:
1. Identify the doc store provider (notion | confluence | google_docs)
2. Use the appropriate MCP server to create a new document
3. Set the title to the TDD title
4. Place it in the configured parent page/folder
5. Return the canonical URL

Store the URL — it will be linked from the ticket and PR.
